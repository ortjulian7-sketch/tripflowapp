# Copiar a .env.local y completar con los valores del proyecto de Supabase.
# Nunca poner acá la clave de rol de servicio (service_role): esa clave vive
# únicamente como secreto de la Supabase Edge Function `delete-account`.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
